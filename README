MINICIPHER - 文件加密/解密工具
===================================

这是一个主要由DeepSeek开发的文件加密/解密GUI工具，支持多种加密算法。本工具提供简单易用的图形界面，让用户可以轻松地对文件进行加密和解密操作。

## 新功能亮点

* **配置文件系统**：支持用户自定义初始设置和默认文件夹
  - 跨平台配置文件管理（Windows、macOS、Linux）
  - 可配置默认加密算法和密钥类型
  - 记住上次使用的文件夹
  - 自定义密码强度要求

* **多语言支持**：支持中文和英文界面
  - 简体中文 (zh_CN) - 默认语言
  - 英文 (en_US)
  - 可通过菜单动态切换语言
  - 完整的国际化字符串管理

项目特点
=========

* 支持两种加密算法：
  - OTP（一次性密码本）：理论上不可破解的加密算法，密钥长度等于文件长度
  - AES256-GCM：高级加密标准，256位密钥，GCM认证模式，兼顾性能与安全

* 两种密钥模式：
  - 随机密钥模式：生成随机密钥文件，需要妥善保管密钥文件
  - 密码模式：使用用户设定的密码，通过PBKDF2派生密钥，无需额外密钥文件

* 完整功能：
  - 图形用户界面（基于tkinter）
  - 配置文件管理（跨平台）
  - 多语言支持（中英文）
  - 文件拖放支持（部分系统）
  - 密码强度验证
  - 完整的错误处理和用户反馈

系统要求
=========

* Python 3.7 或更高版本
* 依赖包：cryptography >= 42.0.0
* tkinter（通常Python自带，部分系统可能需要单独安装）
* 跨平台支持：Windows、macOS、Linux

快速开始
=========

1. 安装依赖：
   ```bash
   pip install -r requirements.txt
   ```

2. 运行程序：
   ```bash
   python main.py
   ```

3. 或者使用构建的可执行文件：
   ```bash
   python build.py --all
   ```
   然后运行：
   * Windows: launch.bat
   * macOS/Linux: ./launch.command 或 ./dist/Cipher

配置文件系统
=============

miniCipher现在支持配置文件系统，配置文件位于：

* **Windows**: `%APPDATA%\miniCipher\config.json`
* **macOS**: `~/Library/Application Support/miniCipher/config.json`
* **Linux/Unix**: `~/.config/miniCipher/config.json`

### 配置选项

配置文件支持以下选项：

```json
{
  "version": "1.0",
  "ui": {
    "language": "zh_CN",        // 界面语言：zh_CN 或 en_US
    "theme": "default",
    "window_width": 800,
    "window_height": 600
  },
  "encryption": {
    "default_algorithm": "OTP", // 默认算法：OTP 或 AES256
    "default_key_type": "random", // 默认密钥类型：random 或 password
    "password_min_length": 8,   // 密码最小长度
    "require_strong_password": true // 是否要求强密码
  },
  "paths": {
    "default_input_dir": "",    // 默认输入目录
    "default_output_dir": "",   // 默认输出目录
    "remember_last_folder": true, // 是否记住上次使用的文件夹
    "last_input_folder": "",    // 上次使用的输入文件夹
    "last_output_folder": ""    // 上次使用的输出文件夹
  }
}
```

多语言支持
===========

### 切换语言

1. 通过菜单栏的"语言"菜单切换：
   - "简体中文"：切换到中文界面
   - "English"：切换到英文界面

2. 语言设置会自动保存到配置文件，下次启动时保持所选语言。

### 支持的语言

| 语言代码 | 语言名称 | 状态 |
|----------|----------|------|
| zh_CN    | 简体中文 | ✅ 完整支持 |
| en_US    | 英文     | ✅ 完整支持 |

使用方法
=========

1. 启动程序后会显示图形界面

2. 选择加密算法：
   - OTP：适用于小文件，理论上不可破解
   - AES256：适用于各种文件大小，性能良好

3. 选择密钥类型：
   - 随机密钥：会生成单独的密钥文件
   - 密码：使用密码加密，无需额外文件

4. 加密文件：
   - 选择输入文件
   - 选择输出目录
   - 点击"开始加密"

5. 解密文件：
   - 选择加密后的文件（.enc格式）
   - 提供密钥文件或密码（根据加密时的设置）
   - 选择输出目录
   - 点击"开始解密"

文件格式说明
=============

* OTP加密文件：.enc扩展名，纯密文数据
* AES加密文件：.enc扩展名，包含算法标识、IV和认证标签
* 密钥文件：
  - OTP密钥：.txt格式，十六进制字符串
  - AES随机密钥：.key格式，二进制数据

* 密码模式：盐值存储在加密文件中，只需记住密码即可解密

测试
====

项目包含完整的单元测试和集成测试，确保功能正确性：

### 运行核心加密测试：
```bash
python test_cipher.py
```

### 运行集成测试（配置文件+多语言）：
```bash
python test_integration_fixed.py
```

### 运行简单语言测试：
```bash
python test_simple.py
```

### 测试包括：
* OTP加密解密测试
* AES256随机密钥加密解密测试
* AES256密码加密解密测试
* 文件格式处理测试
* 向后兼容性测试
* 配置文件系统测试
* 多语言支持测试
* GUI集成测试

构建可执行文件
===============

使用PyInstaller打包为独立可执行文件：

运行构建脚本：
```bash
python build.py --all
```

构建选项：
* `--install-deps`：安装依赖
* `--build`：执行构建
* `--clean`：清理旧的构建文件
* `--test`：测试构建结果
* `--all`：完整流程（安装依赖、构建、测试）

构建完成后，可以使用以下启动脚本：
* Windows: launch.bat
* macOS/Linux: launch.command

持续集成/持续部署 (CI/CD)
========================

项目使用GitHub Actions实现自动构建和发布：

自动构建工作流
-------------

1. **CI构建 (build.yml)**: 每次推送到main分支或创建pull request时自动运行
   - 在Windows、macOS和Linux上构建可执行文件
   - 运行单元测试
   - 生成构建产物作为GitHub Actions artifacts

2. **发布工作流 (release.yml)**: 创建git tag时自动运行
   - 自动构建所有平台的可执行文件
   - 创建GitHub Release
   - 上传构建产物到Release页面

下载预构建版本
--------------

您可以从GitHub Releases页面下载预构建的可执行文件：
- Windows: `Cipher-windows-x64.exe`
- macOS: `Cipher-macos-universal` (通用二进制，支持x64和arm64)
- Linux: `Cipher-linux-x64`

构建产物命名
-----------

构建产物遵循以下命名约定：
- `Cipher-windows-x64.exe` - Windows 64位可执行文件
- `Cipher-macos-universal` - macOS通用二进制文件
- `Cipher-linux-x64` - Linux 64位可执行文件

版本管理
-------

版本号自动生成，遵循语义化版本：
- 主版本.次版本.补丁版本+构建元数据
- 示例：`v1.0.0+dev.2026-02-21.abc123`

本地测试工作流
-------------

要在本地测试GitHub Actions工作流，可以安装`act`工具：
```bash
# 安装act (需要Docker)
# Windows: choco install act-cli
# macOS: brew install act
# Linux: 参考 https://github.com/nektos/act

# 运行CI工作流
act

# 运行发布工作流
act -W .github/workflows/release.yml
```

项目结构
=========

```
miniCipher/
├── main.py                    # 主程序入口
├── cipher_gui.py              # GUI主界面（支持多语言）
├── cipher_algorithms.py       # 加密算法实现
├── config_manager.py          # 配置文件管理系统
├── translations.py            # 多语言翻译模块
├── requirements.txt           # Python依赖
├── build.py                   # 构建脚本
├── test_cipher.py             # 核心加密测试
├── test_integration_fixed.py  # 集成测试
├── test_simple.py             # 简单语言测试
├── launch.bat                 # Windows启动脚本
├── launch.command             # macOS/Linux启动脚本
├── README                     # 说明文档
└── LICENSE                    # 许可证文件
```

开发指南
=========

### 添加新语言

1. 在 `translations.py` 的 `TranslationKeys` 枚举中添加新翻译键
2. 在 `_get_chinese_translations()` 和 `_get_english_translations()` 方法中添加翻译
3. 在 `Language` 枚举中添加新语言代码
4. 在 `get_available_languages()` 和 `get_current_language_display_name()` 中添加语言显示名称

### 添加新配置选项

1. 在 `config_manager.py` 的 `_get_default_config()` 方法中添加默认值
2. 在 `ConfigurationManager` 类中添加相应的getter/setter方法
3. 在GUI中实现相应的设置界面

许可证
======

本项目采用MIT许可证。详见LICENSE文件。

注意事项
========

* OTP加密的密钥长度必须等于文件长度，请妥善保管密钥文件
* AES256密码模式要求密码至少8位，包含大小写字母和数字（可配置）
* 请勿丢失密钥文件或忘记密码，否则无法解密文件
* 建议对重要文件进行备份
* 配置文件会自动创建，首次运行时使用默认配置

版本信息
========

当前版本：1.0 (支持配置系统和多语言)
主要新功能：
1. 配置文件系统 - 支持用户自定义设置
2. 多语言支持 - 中英文界面切换
3. 增强的错误处理和用户体验

最后更新：2026年2月
作者：snoworwind
GitHub: https://github.com/snoworwind/miniCipher